const express = require('express');
const router = express.Router();

// Placeholder for products routes
router.get('/', (req, res) => res.json({ message: "Products route" }));

module.exports = router;
