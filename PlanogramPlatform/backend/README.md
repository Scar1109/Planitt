# Planogram Platform Backend

The backend service for the Planogram Management Platform.

## Prerequisites

- Node.js (v18+)

## How to Run

1.  **Install Dependencies**:
    ```bash
    npm install
    ```

2.  **Start Server**:
    ```bash
    npm start
    ```
    The server runs on **Port 3000** by default.
