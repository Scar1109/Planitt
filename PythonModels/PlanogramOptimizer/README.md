# Planogram Optimizer

A FastAPI service for optimizing retail planograms.

## How to Run

1.  **Install Dependencies**:
    ```bash
    pip install -r requirements.txt
    ```

2.  **Start Service**:
    ```bash
    python -m uvicorn main:app
    ```
