# Inventory Forecasting

A FastAPI service for inventory forecasting models.

## How to Run

1.  **Install Dependencies**:
    ```bash
    pip install -r requirements.txt
    ```

2.  **Start Service**:
    ```bash
    python -m uvicorn main:app
    ```
